name: RecipeRAG
channels:
  - defaults
dependencies:
  - python=3.11
  - fastapi
  - uvicorn
  - chromadb
  - pydantic
  - haystack-ai
  - tqdm
  - sentence-transformers[version='>=2.2.0']
  - tokenizers
  - transformers[version='>=4.36.0']
  - pip
  - pip:
      - chroma-haystack
prefix: /home/duke_trystan/miniconda3/envs/RecipeRAG
